# ========= Secrets (à renseigner en local / prod) =========
OPENAI_API_KEY=sk-REPLACE_ME
TAVILY_API_KEY=tvly-REPLACE_ME

# ========= Paths (optionnel) =========
# Dans Docker, on conseille /app/data/...
DOCS_DIR=/app/data/Corpus
RESPONSES_CSV=/app/data/Students/responses.csv

# ========= Modèle (optionnel) =========
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0
